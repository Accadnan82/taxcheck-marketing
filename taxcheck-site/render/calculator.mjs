import { esc, icons } from '../lib/html.mjs';
import { href, APP_URL, pageHero, ctaBand } from '../lib/layout.mjs';

// Free UAE tax penalty estimator. Route-scoped CSS and JS are inlined here so the
// shared /site.css hash (and therefore every other page's cache) is untouched.
// All legal constants live in the engine below and are documented there.

const ENGINE = "\n/* TaxCheck \u2014 UAE tax penalty estimator.\n   Legal constants verified 2026-09-13 against:\n   - Cabinet Decision 75/2023 (consolidated with amendments, mof.gov.ae) \u2014 Corporate Tax table\n   - Cabinet Decision 129/2025, in force 14 Apr 2026 \u2014 unified 14%/annum late payment (CT + VAT)\n   - Cabinet Decision 49/2021 amending CD 40/2017 \u2014 VAT registration & filing penalties\n   Each month or part thereof counts as a full month. No compounding. */\n(function () {\n  'use strict';\n\n  var T = window.TC_T;\n  var AR = window.TC_LANG === 'ar';\n\n  var CT_FILE_EARLY = 500;      // AED per month, months 1-12\n  var CT_FILE_LATE = 1000;      // AED per month, month 13 onwards\n  var CT_FILE_SWITCH = 12;\n  var CT_REG = 10000;\n  var VAT_REG = 10000;\n  var VAT_FILE_FIRST = 1000;\n  var VAT_FILE_REPEAT = 2000;\n  var LATE_PAY_ANNUAL = 0.14;\n  var REGIME_START = new Date(2026, 3, 14); // 14 Apr 2026 \u2014 CD 129/2025 in force\n\n  /* ---------- dates ---------- */\n\n  function parseDate(v) {\n    if (!v) return null;\n    var p = v.split('-');\n    if (p.length !== 3) return null;\n    var d = new Date(+p[0], +p[1] - 1, +p[2]);\n    return isNaN(d.getTime()) ? null : d;\n  }\n\n  function addMonths(d, n) {\n    var t = new Date(d.getFullYear(), d.getMonth() + n, 1);\n    var last = new Date(t.getFullYear(), t.getMonth() + 1, 0).getDate();\n    t.setDate(Math.min(d.getDate(), last));\n    return t;\n  }\n\n  // Commenced months between due and end: one day late counts as one whole month.\n  // Month-end clamped, so 31 Jan -> 28 Feb is 1 month, not 2.\n  function commencedMonths(due, end) {\n    if (!due || !end || end <= due) return 0;\n    var m = 0;\n    while (addMonths(due, m + 1) <= end) m++;\n    return addMonths(due, m).getTime() === end.getTime() ? m : m + 1;\n  }\n\n  /* ---------- formatting ---------- */\n\n  function toArabicDigits(s) {\n    return String(s).replace(/[0-9]/g, function (c) {\n      return '\u0660\u0661\u0662\u0663\u0664\u0665\u0666\u0667\u0668\u0669'[+c];\n    });\n  }\n\n  function num(n) {\n    var s = Math.abs(n - Math.round(n)) < 0.005\n      ? Math.round(n).toLocaleString('en-US')\n      : n.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });\n    return AR ? toArabicDigits(s) : s;\n  }\n\n  function money(n) {\n    return AR ? (num(n) + ' ' + T.currency) : (T.currency + ' ' + num(n));\n  }\n\n  function digits(n) { return AR ? toArabicDigits(n) : String(n); }\n\n  // Arabic month agreement: 1 \u0634\u0647\u0631 \u0648\u0627\u062d\u062f \u00b7 2 \u0634\u0647\u0631\u0627\u0646 \u00b7 3-10 \u0623\u0634\u0647\u0631 \u00b7 11+ \u0634\u0647\u0631\u0627\u064b\n  function months(n) {\n    if (!AR) return n === 1 ? '1 month' : n + ' months';\n    if (n === 1) return '\u0634\u0647\u0631 \u0648\u0627\u062d\u062f';\n    if (n === 2) return '\u0634\u0647\u0631\u0627\u0646';\n    if (n >= 3 && n <= 10) return toArabicDigits(n) + ' \u0623\u0634\u0647\u0631';\n    return toArabicDigits(n) + ' \u0634\u0647\u0631\u0627\u064b';\n  }\n\n  /* ---------- dom ---------- */\n\n  function $(id) { return document.getElementById(id); }\n  function taxType() { return document.querySelector('input[name=\"tax\"]:checked').value; }\n  function on(id) { return $(id).checked; }\n\n  function syncChrome() {\n    document.querySelectorAll('.segbar label').forEach(function (l) {\n      l.classList.toggle('on', l.querySelector('input').checked);\n    });\n    document.querySelectorAll('.checks label').forEach(function (l) {\n      l.classList.toggle('on', l.querySelector('input').checked);\n    });\n    var isVat = taxType() === 'vat';\n    $('repeat-wrap').classList.toggle('hidden', !(isVat && on('sc-file')));\n    $('filed-wrap').classList.toggle('hidden', !on('sc-file'));\n    $('paid-wrap').classList.toggle('hidden', !on('sc-pay'));\n    $('tax-wrap').classList.toggle('hidden', !on('sc-pay'));\n    $('dates-wrap').classList.toggle('hidden', !(on('sc-file') || on('sc-pay')));\n  }\n\n  /* ---------- calculation ---------- */\n\n  function calc() {\n    var ct = taxType() === 'ct';\n    var due = parseDate($('due').value);\n    var filed = parseDate($('filed').value);\n    var paid = parseDate($('paid').value);\n    var tax = parseFloat(String($('tax').value).replace(/[^0-9.]/g, '')) || 0;\n    var rows = [];\n    var notes = [];\n    var perMonth = 0;\n\n    if (on('sc-reg')) {\n      rows.push({\n        label: T.rowReg,\n        src: ct ? T.srcCtReg : T.srcVatReg,\n        amount: ct ? CT_REG : VAT_REG\n      });\n      if (ct) notes.push({ kind: 'info', title: T.waiverTitle, body: T.waiverBody });\n    }\n\n    var mFile = 0;\n    if (on('sc-file') && due && filed) {\n      mFile = commencedMonths(due, filed);\n      if (mFile > 0) {\n        if (ct) {\n          var early = Math.min(mFile, CT_FILE_SWITCH);\n          var late = Math.max(mFile - CT_FILE_SWITCH, 0);\n          var amt = early * CT_FILE_EARLY + late * CT_FILE_LATE;\n          rows.push({\n            label: T.rowFileCt.replace('{m}', months(mFile)),\n            src: T.srcCtFile,\n            amount: amt\n          });\n          perMonth += (mFile + 1 <= CT_FILE_SWITCH) ? CT_FILE_EARLY : CT_FILE_LATE;\n        } else {\n          var rep = on('repeat');\n          rows.push({\n            label: rep ? T.rowFileVatRepeat : T.rowFileVat,\n            src: T.srcVatFile,\n            amount: rep ? VAT_FILE_REPEAT : VAT_FILE_FIRST\n          });\n        }\n      }\n    }\n\n    var mPay = 0;\n    if (on('sc-pay') && due && paid && tax > 0) {\n      mPay = commencedMonths(due, paid);\n      if (mPay > 0) {\n        var pen = tax * (LATE_PAY_ANNUAL / 12) * mPay;\n        rows.push({\n          label: T.rowPay.replace('{m}', months(mPay)).replace('{t}', money(tax)),\n          src: T.srcPay,\n          amount: pen\n        });\n        perMonth += tax * (LATE_PAY_ANNUAL / 12);\n      }\n    }\n\n    if (due && due < REGIME_START) {\n      notes.push({ kind: 'warn', title: T.oldRegimeTitle, body: T.oldRegimeBody });\n    }\n\n    return { rows: rows, notes: notes, perMonth: perMonth, total: rows.reduce(function (s, r) { return s + r.amount; }, 0) };\n  }\n\n  /* ---------- render ---------- */\n\n  function render() {\n    syncChrome();\n    var r = calc();\n    var b = '';\n\n    r.notes.forEach(function (n) {\n      b += '<div class=\"banner ' + n.kind + '\"><strong>' + n.title + '</strong>' + n.body + '</div>';\n    });\n    $('banners').innerHTML = b;\n\n    if (!r.rows.length) {\n      $('out').innerHTML = '<p class=\"empty\">' + T.emptyState + '</p>';\n      return;\n    }\n\n    var h = '<table class=\"bd\"><thead><tr><th>' + T.thItem + '</th><th class=\"num\">' + T.thAmount + '</th></tr></thead><tbody>';\n    r.rows.forEach(function (row) {\n      h += '<tr><td>' + row.label + '<span class=\"src\">' + row.src + '</span></td>'\n        + '<td class=\"num\">' + money(row.amount) + '</td></tr>';\n    });\n    h += '<tr class=\"total\"><td>' + T.thTotal + '</td><td class=\"num\">' + money(r.total) + '</td></tr>';\n    h += '</tbody></table>';\n    if (r.perMonth > 0) {\n      h += '<p class=\"per-month\">' + T.perMonth.replace('{a}', '<b>' + money(r.perMonth) + '</b>') + '</p>';\n    }\n    $('out').innerHTML = h;\n  }\n\n  /* ---------- wiring ---------- */\n\n  var paidTouched = false;\n\n  function init() {\n    document.querySelectorAll('input[name=\"tax\"], .checks input, #due, #filed, #paid, #tax, #repeat')\n      .forEach(function (el) {\n        el.addEventListener('change', render);\n        el.addEventListener('input', render);\n      });\n\n    $('paid').addEventListener('input', function () { paidTouched = true; });\n    $('filed').addEventListener('input', function () {\n      if (!paidTouched) $('paid').value = $('filed').value;\n    });\n\n    render();\n  }\n\n  if (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', init);\n  } else {\n    init();\n  }\n\n  // exposed for headless testing\n  window.TC_CALC = { commencedMonths: commencedMonths, addMonths: addMonths, months: months, calc: calc };\n})();\n\n";

const STYLE = "<style>\n/* Calculator-only supplement.\n   The page LINKS the live site stylesheet at /site.css, so every token\n   (--bg, --ink, --teal, --gold, --radius, --display, --body, the html[lang=ar]\n   Tajawal override, .hdr, .wrap, .card, .btn, .field, .tbl, .tag, .note, .lede,\n   .eyebrow, .pill, .ftr \u2026) comes from the site itself and follows it through\n   future redesigns. Nothing below redefines a token \u2014 it only adds the few\n   components the calculator needs that the site does not already have. */\n/* \u2014 layout \u2014 */\n.pg-penalty-calculator .calc-head { padding: 64px 0 8px; }\n.pg-penalty-calculator .calc-main { padding-bottom: 72px; }\n.pg-penalty-calculator .calc-grid { display: grid; gap: 18px; }\n@media (max-width: 720px) {\n.pg-penalty-calculator .calc-head { padding-top: 40px; }\n}\n/* \u2014 fieldsets \u2014 */\n.pg-penalty-calculator .calc fieldset { border: 0; margin: 0 0 22px; padding: 0; }\n.pg-penalty-calculator .calc fieldset:last-child { margin-bottom: 0; }\n.pg-penalty-calculator .calc legend {\n  padding: 0; margin-bottom: 12px;\n  font-size: 11.5px; letter-spacing: 0.08em; text-transform: uppercase;\n  color: var(--muted); font-weight: 600;\n}\n.pg-penalty-calculator .calc .grid3 { display: grid; grid-template-columns: repeat(auto-fit, minmax(190px, 1fr)); gap: 14px; }\n.pg-penalty-calculator .calc .hint { font-size: 12px; color: var(--muted); }\n/* \u2014 segmented control (tax type) \u2014 */\n.pg-penalty-calculator .segbar { display: inline-flex; padding: 4px; gap: 4px; border-radius: 14px;\n          border: 1px solid var(--line-2); background: rgba(255,255,255,0.04); flex-wrap: wrap; }\n.pg-penalty-calculator .segbar label {\n  display: inline-flex; align-items: center; justify-content: center;\n  padding: 9px 18px; border-radius: 10px; cursor: pointer;\n  font-weight: 600; font-size: 14px; color: var(--ink-2);\n  transition: background .2s, color .2s;\n}\n.pg-penalty-calculator .segbar input { position: absolute; opacity: 0; width: 0; height: 0; pointer-events: none; }\n.pg-penalty-calculator .segbar label:hover { color: var(--ink); }\n.pg-penalty-calculator .segbar label.on {\n  background: linear-gradient(135deg, var(--gold-2), var(--gold));\n  color: #1A1200; box-shadow: 0 8px 22px rgba(245,185,74,0.22);\n}\n/* \u2014 scenario rows \u2014 */\n.pg-penalty-calculator .checks { display: grid; gap: 10px; }\n.pg-penalty-calculator .checks label {\n  display: flex; gap: 12px; align-items: flex-start; cursor: pointer;\n  padding: 14px 16px; border-radius: 14px;\n  border: 1px solid var(--line); background: rgba(255,255,255,0.03);\n  transition: border-color .2s, background .2s;\n}\n.pg-penalty-calculator .checks label:hover { border-color: var(--line-2); }\n.pg-penalty-calculator .checks label.on { border-color: rgba(46,211,198,0.45); background: rgba(46,211,198,0.07); }\n.pg-penalty-calculator .checks input { margin: 3px 0 0; flex: none; width: 16px; height: 16px; accent-color: var(--teal); }\n.pg-penalty-calculator .checks .t { font-weight: 600; font-size: 14.5px; color: var(--ink); }\n.pg-penalty-calculator .checks .d { display: block; font-size: 12.5px; color: var(--muted); font-weight: 400; margin-top: 2px; }\n.pg-penalty-calculator .hidden { display: none !important; }\n/* \u2014 banners \u2014 */\n.pg-penalty-calculator .banner {\n  border-radius: 14px; padding: 14px 16px; font-size: 13.5px; margin-bottom: 14px;\n  border: 1px solid var(--line-2); background: rgba(255,255,255,0.03); color: var(--ink-2);\n}\n.pg-penalty-calculator .banner strong {\n  display: block; margin-bottom: 4px; font-family: var(--display);\n  font-weight: 700; font-size: 15px; color: var(--ink);\n}\n.pg-penalty-calculator .banner.info { border-color: rgba(46,211,198,0.38); background: rgba(46,211,198,0.07); }\n.pg-penalty-calculator .banner.info strong { color: var(--teal-2); }\n.pg-penalty-calculator .banner.warn { border-color: rgba(255,179,92,0.38); background: rgba(255,179,92,0.07); }\n.pg-penalty-calculator .banner.warn strong { color: var(--warn); }\n/* \u2014 results \u2014 */\n.pg-penalty-calculator .bd { width: 100%; border-collapse: collapse; font-size: 14.5px; }\n.pg-penalty-calculator .bd th {\n  text-align: start; padding: 10px 12px;\n  font-size: 11.5px; letter-spacing: 0.08em; text-transform: uppercase;\n  color: var(--muted); font-weight: 600; background: rgba(255,255,255,0.03);\n  border-bottom: 1px solid var(--line);\n}\n.pg-penalty-calculator .bd th:last-child, .pg-penalty-calculator .bd td.num { text-align: end; }\n.pg-penalty-calculator .bd td {\n  padding: 14px 12px; vertical-align: top;\n  border-bottom: 1px solid var(--line); color: var(--ink);\n}\n.pg-penalty-calculator .bd td.num { white-space: nowrap; font-variant-numeric: tabular-nums; font-weight: 600; }\n.pg-penalty-calculator .bd .src { display: block; margin-top: 4px; font-size: 12px; color: var(--muted); font-weight: 400; }\n.pg-penalty-calculator .bd tr.total td {\n  border-bottom: 0; border-top: 1px solid var(--line-2);\n  padding-top: 18px; font-family: var(--display); font-weight: 800; font-size: 24px;\n}\n.pg-penalty-calculator .bd tr.total td.num { color: var(--gold-2); }\n.pg-penalty-calculator .per-month { margin: 16px 0 0; font-size: 13.5px; color: var(--muted); }\n.pg-penalty-calculator .per-month b { color: var(--ink); font-weight: 600; }\n.pg-penalty-calculator .empty { color: var(--muted); font-size: 14px; margin: 0; }\n/* \u2014 FAQ \u2014 */\n.pg-penalty-calculator .faqs { display: grid; gap: 10px; }\n.pg-penalty-calculator details.faq {\n  border: 1px solid var(--line); border-radius: 14px;\n  background: rgba(255,255,255,0.03); padding: 16px 18px;\n}\n.pg-penalty-calculator details.faq[open] { border-color: var(--line-2); }\n.pg-penalty-calculator details.faq summary {\n  cursor: pointer; list-style: none; display: flex; gap: 12px; align-items: flex-start;\n  font-weight: 600; font-size: 15px; color: var(--ink);\n}\n.pg-penalty-calculator details.faq summary::-webkit-details-marker { display: none; }\n.pg-penalty-calculator details.faq summary::before { content: \"+\"; color: var(--teal); font-weight: 800; flex: none; width: 14px; }\n.pg-penalty-calculator details.faq[open] summary::before { content: \"\\2212\"; }\n.pg-penalty-calculator details.faq p { margin: 10px 0 0 26px; font-size: 14px; color: var(--ink-2); }\nhtml[dir=\"rtl\"] .pg-penalty-calculator details.faq p { margin: 10px 26px 0 0; }\n/* \u2014 disclaimer \u2014 */\n.pg-penalty-calculator .disclaimer {\n  margin: 26px 0; padding: 18px 20px; border-radius: 16px;\n  border: 1px solid var(--line); background: rgba(255,255,255,0.025);\n  font-size: 13px; color: var(--muted); line-height: 1.65;\n}\n.pg-penalty-calculator .disclaimer b { color: var(--ink-2); }\n@media print {\n.pg-penalty-calculator .sky, .pg-penalty-calculator .grain, .pg-penalty-calculator .hdr, .pg-penalty-calculator .cta-band, .pg-penalty-calculator .announce { display: none !important; }.pg-penalty-calculator body { background: #fff; color: #000; }\n}\n</style>";

export function renderCalculator({ lang, t }) {
  const c = t.calc;

  const faq = c.faq.map((f) =>
    `<details class="faq"><summary>${esc(f.q)}</summary><p>${esc(f.a)}</p></details>`).join('');

  const faqLd = {
    '@context': 'https://schema.org', '@type': 'FAQPage',
    mainEntity: c.faq.map((f) => ({
      '@type': 'Question', name: f.q,
      acceptedAnswer: { '@type': 'Answer', text: f.a },
    })),
  };

  const form = `
<div class="card calc">
  <fieldset>
    <legend>${esc(c.legendTax)}</legend>
    <div class="segbar">
      <label class="on"><input type="radio" name="tax" value="ct" checked>${esc(c.taxCt)}</label>
      <label><input type="radio" name="tax" value="vat">${esc(c.taxVat)}</label>
    </div>
  </fieldset>

  <fieldset>
    <legend>${esc(c.legendScenario)}</legend>
    <div class="checks">
      <label><input type="checkbox" id="sc-reg"><span><span class="t">${esc(c.scReg)}</span><span class="d">${esc(c.scRegD)}</span></span></label>
      <label class="on"><input type="checkbox" id="sc-file" checked><span><span class="t">${esc(c.scFile)}</span><span class="d">${esc(c.scFileD)}</span></span></label>
      <label class="on"><input type="checkbox" id="sc-pay" checked><span><span class="t">${esc(c.scPay)}</span><span class="d">${esc(c.scPayD)}</span></span></label>
    </div>
  </fieldset>

  <fieldset id="dates-wrap">
    <legend>${esc(c.legendDates)}</legend>
    <div class="grid3">
      <div class="field"><label for="due">${esc(c.lblDue)}</label>
        <input type="date" id="due" value="2026-09-30"><span class="hint">${esc(c.hintDue)}</span></div>
      <div class="field" id="filed-wrap"><label for="filed">${esc(c.lblFiled)}</label>
        <input type="date" id="filed" value="2026-12-15"></div>
      <div class="field" id="paid-wrap"><label for="paid">${esc(c.lblPaid)}</label>
        <input type="date" id="paid" value="2026-12-15"><span class="hint">${esc(c.hintPaid)}</span></div>
      <div class="field" id="tax-wrap"><label for="tax">${esc(c.lblTax)}</label>
        <input type="text" id="tax" inputmode="decimal" value="200000"><span class="hint">${esc(c.hintTax)}</span></div>
    </div>
    <div class="checks" id="repeat-wrap" style="margin-top:14px">
      <label><input type="checkbox" id="repeat"><span><span class="t">${esc(c.lblRepeat)}</span><span class="d">${esc(c.lblRepeatD)}</span></span></label>
    </div>
  </fieldset>
</div>`;

  const body = `
<section class="calc-main">
  <div class="wrap calc-grid">
    ${form}
    <div id="banners"></div>
    <div class="card">
      <h2 style="font-size:22px;margin:0 0 14px">${esc(c.resultsH)}</h2>
      <div id="out"></div>
    </div>
    <div class="disclaimer">${c.disclaimer}</div>
    <div>
      <h2 style="font-size:22px;margin:8px 0 14px">${esc(c.faqH)}</h2>
      <div class="faqs">${faq}</div>
    </div>
  </div>
</section>`;

  const script = `
<script type="application/ld+json">${JSON.stringify(faqLd)}</script>\n<script>window.TC_LANG=${JSON.stringify(lang)};window.TC_T=${JSON.stringify(c)};</script>\n<script>` + ENGINE + `</script>`;

  const ctas = `<a class="btn btn-gold btn-lg" href="${APP_URL}">${esc(t.ctaDemo)} ${icons.arrow(18)}</a>`
             + `<a class="btn btn-ghost btn-lg" href="${href(lang, 'review')}">${esc(t.labels.review)}</a>`;

  return STYLE
    + pageHero({ kicker: c.kicker, title: c.h1Html, sub: c.sub, ctas })
    + body
    + ctaBand(lang, t, c.ctaH, c.ctaP, t.ctaDemo, APP_URL, t.labels.demo, href(lang, 'demo'))
    + script;
}
